# 다음 작업과 다른 PC에서 이어가기

갱신: 2026-09-24

## 읽는 순서

README.md → RESEARCH_CONTEXT.md → NEXT_STEPS.md → RESEARCH_ALIGNMENT.md.
연구계획서 원문 요약과 대화에서 나온 제안은 RESEARCH_CONTEXT.md에서 구별한다.

## 완료

- 자동 4국면, 관절각·전후 변화, 그래프별 해석, 좌표 재생 및 화면 정리.
- 얼굴 스틸컷 표시 제거, 손목 Y 표시 연결 복원, 좌표 표/CSV의 눈·귀·코·손가락 필터.
- Gemini 순차 테스트 스크립트와 quota 중단, 테스트 44개 통과 기록.
- 코드 5658a7b, 이전 README 040e749: 사용자 push 로그로 GitHub main 업로드 확인.
- 맥에서 40f80b1까지 동기화. 최신 실행 기록은 PROGRESS.md 참고.
- 2026-09-24 `gemini-3.5-flash-lite` 짧은 직접 API 요청 성공. 영상 분석 실연동은 미검증. Mock 기본값 유지.
- 다른 PC의 `.env`는 동기화되지 않으므로 `GEMINI_MODEL=gemini-3.5-flash-lite`를 개별 설정.

## 다음 대화의 시작점

RESEARCH_CONTEXT.md의 「후속 대화에서 명확해진 최종 방향」까지 읽는다. 최종 목표는 CCTV 외부 관측과 IoT·제어 보고를 대조하는 피지컬 AI 보안이다. 사람의 상태 추론 → 산업 작업 부하 → 로봇 이상 탐지 사이에서 각 단계의 지지/기각 기준과 필요한 데이터를 먼저 정한다. 골프 실증만으로 로봇 보안이 입증됐다고 하지 않는다.

## 우선순위

1. **실험 입력 명세 확정**: 전체 좌표 시계열+각도, 신뢰도, 결측/보간 출처, 전송 제외 필드. 4국면 요약과 전체 시계열을 동일 조건으로 취급하지 않는다.
2. **계산 학습·검증**: 한 영상의 세 점으로 각도 직접 계산, 국면 시점 수기 대조, FPS와 각속도 계산 확인. 국면 감지의 고정 임계값·프레임 규칙과 계획서 설명 차이 기록.
3. **출력 스키마**: 각도 추정치, 부담 부위, 질환 서술, 근거, 코드 후보, 매핑 불가를 분리. 원문·불확실성 보존. 질환 서술을 요구한다고 실제 진단을 단정하지 않게 한다.
4. **작은 API 실행**: 1~2개 영상으로 입력 크기·비용·응답 확인. 기존 scripts/test_gemini_api.py는 --start N부터 5번까지 실행하므로 1~2개 전용 옵션은 추가 구현이 필요하다. 본 단계는 논문 결론용 표본이 아니다.
5. **파일럿 설계**: 익명 피험자 ID와 반복 스윙 ID, 촬영 방향/FPS, 코드 버전·모델·프롬프트·설정·원응답을 기록. 동일인 반복 시행과 동일 입력 재호출의 일관성은 별도로 집계한다.
6. **검증 지표**: 재현성, 피험자 간 변별력, 코드 매핑 일관성, 입력 근거 충실성, 판정 보류·결측률. 동일 응답 비율만으로 정확한 민감정보 유출을 입증했다고 하지 않는다.
7. **RULA·REBA**: 사용자가 논의한 적용표 수령 후 산출 각도에서 평가 항목으로의 대응 확인. 추가 입력·운동면·보정 조건 없이 최종 점수를 만들지 않는다.

## 후속/선택 작업

- 여러 영상 업로드 → 순차 분석 → 전체 목록 / 영상별 상세 / 통합 비교. 현재 앱은 단일 영상이다.
- 예산 허용 시 영상 / 좌표 / 요약 3조건 비교. 원문 필수 실험이 아니다.
- 확인할 구간을 접힌 표시로 전환. 현재 펼쳐져 있다.
- 피지컬 AI는 설계 범위. 로봇 실증, CCTV 실시간 경보, 마모/침해 구분은 별도 데이터 확보 이후.

## 맥북에서 재개

1. 저장소를 clone하거나 기존 저장소에서 로컬 변경을 확인한 뒤 `git pull --ff-only`로 갱신한다.
2. Windows의 `.venv`와 `start.bat`는 맥에서 재사용하지 않는다. 맥용 Python 환경을 새로 만들고 의존성 호환성을 확인한다. Windows에서 고정한 lock 파일의 맥 호환성은 미검증이다.
3. 모델 파일 `models/pose_landmarker.task`, 샘플 영상, `.env`는 별도 준비한다. 이 파일들과 결과 outputs/는 Git에서 제외된다. 원본 연구계획서 PDF도 저장소에 없다.
4. Codex에 아래 요청을 붙여넣는다.

> README.md, RESEARCH_CONTEXT.md, NEXT_STEPS.md, RESEARCH_ALIGNMENT.md를 먼저 읽고 MotionGuard 연구를 이어가줘. 연구 핵심은 좌표·각도에서의 미제공 신체정보 추론, 질환 서술 코드 라벨화, 재현성과 변별력 검증이야. RULA·REBA는 산출 각도에 연결하고 피지컬 AI는 설계/후속 범위야. 우선 현재 코드와 문서 차이를 확인하고 다음 실험 입력 명세부터 함께 정리해줘. 계획·가설과 검증 완료 사실을 구분해줘.
