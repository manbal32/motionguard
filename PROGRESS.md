# 진행 로그

## 2026-09-18
- 스틸컷 카드 Wrist Y 표시 수정 (국면 감지용 값 라벨 명시)
- 백스윙탑 국면 각도 출력 누락 수정, 4국면 각도표 추가
- 정면 촬영에서 몸통 기울기 0.5~3.7도 측정 한계 확인
- Gemini 무료 등급 확인, .env 설정 (gemini-2.5-flash-lite, MOCK=0)
  실제 호출 직전 Codex 한도 소진 → 미검증

## 2026-09-24 (맥)
- 회사에서 push한 main을 맥 `~/motionguard-new`에 fast-forward로 동기화 (40f80b1).
- `.env`는 Git 제외 대상이므로 맥에서 별도로 설정. 실제 키는 커밋하지 않음.
- `gemini-2.5-flash-lite` 직접 요청은 404: 신규 사용자에게 제공되지 않는다는 서버 응답 확인.
- `.env` 모델을 `gemini-3.5-flash-lite`로 변경하고 짧은 직접 API 요청 1회 성공 (`OK.` 응답).
- API 연결만 검증 완료. 실제 골프 영상 분석, 앱에서의 실연동 보고서, golf1~5 배치는 아직 미검증.
- 비용 관리 의도에 따라 `MOTIONGUARD_MOCK_GEMINI=1` 및 앱 Mock 기본값 유지. 직접 연결 테스트만 Mock을 거치지 않고 실행.
- 공유 `.env.example`에 새 모델명 반영. 회사 PC의 기존 `.env`는 pull로 바뀌지 않으므로 모델명을 직접 변경해야 함.

## 2026-09-24 (요청 진단 추가)
- 앱에서 quota/rate-limit으로 로컬 측정 결과가 대신 표시된 현상 확인. 정확한 제한 항목은 아직 미확인.
- 다음 실제 요청에서 입력 문자/바이트 수, 국면/시계열 행 수, count_tokens 입력 토큰 수, 성공 응답 usage를 기록하도록 추가.
- 실패 시 HTTP 코드, 서버 QuotaFailure의 제한 항목/값, RetryInfo 대기시간을 표시. 전체 오류 메시지·API 키·좌표 원문은 로그에 저장하지 않음.
- 로컬 로그: outputs/gemini_requests.jsonl (Git 제외). Mock 모드는 API 호출/진단 로그 생성 없음.
- 48개 모의/로컬 테스트 통과. 이번 수정 과정에서는 실제 API를 호출하지 않았으며, 실제 제한 원인은 앱 재시작 후 요청 1회로 확인 필요.

## 2026-09-24 (움직임 해석 시제품)
- 사용자 실제 요청에서 258프레임 / 입력 1,076,731토큰 / 무료 분당 입력 한도 250,000에 따른 429 확인. 영상·이미지 0개.
- 전체 데이터는 로컬 보존, 최대 40개 기준/변화/주변 프레임 + 4국면 통계로 Gemini 입력 축소.
- 좌우별 2D 각도·기준 대비 차이·인접 원본 프레임 각속도·신뢰도·보간 출처 포함. 좌표는 4자리, 측정값은 3자리로 전송하며 시간은 6자리; 로컬 원본은 유지.
- Gemini의 부담 검토/긍정적 특징/판단 보류를 별도 화면에 표시. 인용 프레임과 수치가 맞는지 대조하며 의학적 검증으로 취급하지 않음.
- 앱의 생성은 명시적 AI 보고서 갱신 버튼으로만 실행. 입력 50,000토큰 예산을 초과하거나 토큰 계산 실패 시 생성 차단, 자동 재시도 없음.
- 합성 258프레임 → 40프레임 요청을 실제 토큰 계산 API로 확인: 27,971토큰(당시 프롬프트). 생성 호출·개인 영상 전송 없음.
- 54개 테스트와 초기 화면/모의 해석 화면 AppTest 통과. 개인 영상의 새 모델 해석은 아직 미검증.

## 다음 할 일
- 맥에서 golf1~5 배치 실행 (STEP 5 예비)
- 앱 재시작 후 필요할 때만 Mock을 끄고 실제 영상 보고서 확인
- STEP 7 등급 산출 여부 결정 (A: 가정명시 / B: 계획서수정)
- RULA 원논문 검토
